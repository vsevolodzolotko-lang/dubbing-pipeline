# code_nodes/

Reference copies of JavaScript bodies that live inside n8n **Code** nodes. The authoritative source is the `jsCode` field inside each workflow JSON — these `.js` files mirror that code so it's diff-friendly in PRs and readable in editors with JS support.

After editing any `code_nodes/*.js`, run `npm run sync` (= `node scripts/sync_jscode.js`) to push the change into the workflow JSON. The script is idempotent — re-running on an already-synced state is a no-op. Covers all 28 nodes listed below across W_Master/W1/W2/W3/W3_Dispatch/W_Regen/W_Abort/W_Error.

| File | Lives in workflow | Node name | Purpose |
|---|---|---|---|
| `build_started_slack.js` | W_Master | Build Started Slack Message | Builds the "🚀 Localization started" Block Kit message posted at run start (heads-up: don't touch Drive). Carries a danger button "🛑 Stop localization" with a native confirm dialog; the button's `value` is the run_token. Posted via an HTTP Request node to `chat.postMessage` (blocks need the raw API — the native Slack node can't author a `confirm` dialog). |
| `prepare_run_token.js` | W_Master | Prepare Token Rows | Emits the two config rows the following Sheets `appendOrUpdate` writes: `localization_run_token = archive_run_at` and `localization_abort_token = ''` (clears any stale stop). Scopes the kill-switch to the current run. |
| `check_abort_w3dispatch.js` | W3_Dispatch | Check Abort | Cooperative kill-switch checkpoint between Read Config and the `Aborted?` IF. Re-entered once per language. Emits a sentinel `{aborted,...}`; the IF routes true → Build Stopped Slack (halt + confirmation) / false → Resolve Lang → Fire W3. |
| `build_stopped_slack.js` | W3_Dispatch | Build Stopped Slack | Posts the "✅ Localization stopped — confirmed / safe to start a new run" message when the chain actually halts (Aborted?=true branch). The confirmation that pairs with W_Abort's click-time "🛑 Localization stopped". |
| `w_abort_prep_signature.js` | W_Abort | Prep Signature | Assembles the Slack signature base `v0:{ts}:{raw_body}` (raw body from the webhook's binary — Raw Body must be ON), plus the signing secret and the `X-Slack-Signature` header, for the Crypto (HMAC) node. |
| `w_abort_verify_parse.js` | W_Abort | Verify & Parse Action | Security gate: compares the HMAC against `X-Slack-Signature` (+ 5-min replay guard), then parses the interaction payload. Returns `[]` on any failure (no abort written). On success emits `{ run_token, slack_user, response_url }`. |
| `w_abort_prepare_row.js` | W_Abort | Prepare Abort Row | Emits the `localization_abort_token = run_token` config row for the following Sheets `appendOrUpdate`. |
| `w_abort_build_confirm.js` | W_Abort | Build Confirm Slack | Builds the click-time "🛑 Localization stopped" message (initiator + "awaiting confirmation in the next message") for the native Slack node. The actual-halt confirmation comes later from W3 Dispatch's Build Stopped Slack. |
| `build_error_slack.js` | W_Error | Build Error Slack | Failure notifier. Fired by a global Error Trigger when W_Master/W3_Synthesize_v2/W3_Dispatch/W_Abort die with an uncaught error. Builds "❌ Локалізація впала" with workflow name, failing step, error message, run_token, and execution URL. The pipeline's only negative signal — without it a crashed run is silent. |
| `segment_transcript.js` | W1_STT_and_Segment | Segment Transcript | Parses Deepgram word-level timestamps into sentence-level segments. Pure last-word boundary (no longer stretches `en_end_sec` to `audioDuration` — that's handled via separate `audio_duration_sec` column per `LAST_SEGMENT_TRAILING_SILENCE_SEPARATION`). |
| `prepare_tone_analysis.js` | W2_Translate_v2 | Prepare Tone Analysis | Builds one Claude request body that classifies every segment's `segment_type` (narrative / instruction / movement) + extracts `movement_keywords` and `key_concepts`. The two movement signals feed back into W3 (Expand TTS Jobs → Check Timing + Pad) where they decide whether silence-borrow is allowed. |
| `parse_tone_analysis.js` | W2_Translate_v2 | Parse Tone Map | Extracts JSON from Claude tone-analysis response → one item per `segment_id`. |
| `prepare_and_expand.js` | W2_Translate_v2 | Prepare and Expand | Builds one Claude translate request per pending segment. Injects ToV + tone context. Wraps user content in `<english>...</english>` to stop Claude from interpreting short text as conversation. |
| `extract_translations.js` | W2_Translate_v2 | Extract Translations | Parses Claude's batched translation response, emits one item per segment with `{de, es, fr, pl, pt, it, tr}`. Defensive — skips broken batches with error log. |
| `verify_translations.js` | W2_Translate_v2 | Verify Translations | Cross-checks Claude translations for false-friend / semantic drift via batched Claude QA pass. Bounded concurrency. |
| `gemini_editor.js` | W2_Translate_v2 | Gemini Editor | Cross-model editorial review (Gemini 3.5 Flash via OpenAI-compatible endpoint). Default active editor. Returns text unchanged when clean. |
| `openai_editor.js` | W2_Translate_v2 | OpenAI Editor (orphaned) | GPT-5 alternative editor. Sits on canvas but disconnected by default. Swap in via n8n UI if Gemini quality drops on a specific lesson. |
| `formality_lint.js` | W2_Translate_v2 + W3 Phase 2 expand | Formality Lint | Deterministic post-processor that enforces informal address (du/tu/ty/sen) per lang. |
| `adapt_translations.js` | W2_Translate_v2 | Adapt Translations | CPS-based estimation per language. If estimated duration > en_duration_sec × 1.05, runs up to 3 progressive Claude shorten attempts (light → medium → max). Length floor 60% prevents over-shortening. |
| `expand_tts_jobs.js` | W3_Synthesize_v2 | Expand TTS Jobs | Builds one TTS job per (segment × active lang). Computes slot info: `tts_budget_sec`, `effective_slot_sec`, `max_borrowable_sec`, `trailing_steal_sec`, `tail_audio_silence_sec` (last seg only). Reads `audio_duration_sec` from segments row to expose trailing silence. |
| `check_timing_and_pad.js` | W3_Synthesize_v2 | Check Timing + Pad | Largest node. Measures real PCM duration of initial TTS, runs Gemini 3.5 Flash shorten loop (3 attempts), then dynamic per-voice speed-up (`voice.speed + max_speed_up_delta` ceiling), then hard-truncate. Permissive silence-borrow: any non-movement segment may extend past `en_duration` into trailing silence (bounded by `effective_slot_sec`); movement-locked stays strict. Prepends lead silence, appends tail silence, builds final WAV. Appends `tail_audio_silence_sec − borrowed_sec` to last seg's WAV so sum(per-seg) equals EN total. |
| `phase2_batch_llm_tts.js` | W3_Synthesize_v2 | Phase 2: Batch LLM + TTS | Slowdown-to-fill via Opus 4.7 expansion. Triggered for cells where Phase 1 `real_duration / en_duration < expansion_threshold`. Includes refusal/false-friend safety nets + re-TTS speed-up retry. |
| `trim_lead_for_sequence.js` | W3_Synthesize_v2 | Trim Lead For Sequence | Concat-time alignment fix. Trims `borrowed_sec[N]` bytes from segment N+1's lead silence so per-segment WAVs sum exactly to the full lesson WAV per language. |
| `build_full_audio_per_lang.js` | W3_Synthesize_v2 + W_Regen | Build Full Audio Per Lang | After loop completes: groups all per-segment WAVs by language, sorts by `segment_id`, strips 44-byte WAV headers, concatenates raw PCM, wraps fresh WAV header. Emits 7 binary items (one per active lang). |
| `build_vtt_per_lang.js` | W3_Synthesize_v2 + W_Regen | Build VTT Per Lang | One WebVTT file per active lang. Cue text = `text_translated`; cue timings = `en_start_sec → en_end_sec`. |
| `regen_synthesize.js` | W_Regen | Regen Engine | Reads `needs_retts=TRUE` rows from localizations, re-TTSes via ElevenLabs with Phase 1-style timing logic. Bounded concurrency (`regen_concurrency` config, default 5). On success writes `needs_attention=REVIEW` (yellow); on still-not-fit writes `TRUE`. Returns `[]` if no rows flagged (silent exit). |
| `predelete_drive_files.js` | W_Regen (reference, unused in workflow) | Pre-Save Cleanup | Drive deduplication helper. Calls `this.helpers.httpRequestWithAuthentication` — note that this helper is BLOCKED in Code Node sandbox on some n8n versions; the production W_Regen uses HTTP Request nodes instead. Kept here as documentation of the pattern. |

Inline-only nodes (not yet extracted to `.js`): W1 `Get Params`; W2 `Get Params`; W3 `Get Params`, `Prepare Localization Row`; W_Master nodes (`Parse Filename`, `Once Per Run`, `Plan Sources`, `Plan Archive`, `Plan Subfolders`, `Plan Moves`, `Pass Lessons (×4)`, `Build Slack Message`); W_Regen `Get Params`, `Coalesce Updates`, `Plan Old Deletes Full/VTT`, `Build Regen Slack Message`. All are short (≤2.8KB) and stable — extract on demand if you need to edit one.

## Conventions

- All Code nodes return `[{ json: {...}, binary: {...} }, ...]` items, or `[]` for no-op exit (downstream nodes simply don't fire).
- HTTP calls use `this.helpers.httpRequest` (not `fetch`, not raw `http`).
- `this.helpers.httpRequestWithAuthentication` and `this.getCredentials` are NOT available in Code Node on some n8n versions. For Drive/Sheets API calls that need OAuth, use a dedicated HTTP Request node with `authentication: predefinedCredentialType` + `nodeCredentialType: googleDriveOAuth2Api` (see W_Master archive chain, W_Regen Drive PATCH). For Slack API calls that need the bot token (e.g. `chat.postMessage` with Block Kit blocks), use the same pattern with `nodeCredentialType: slackApi` (see W_Master `Slack Notify (Started)`).
- **Crypto in Code nodes is unreliable** (`require('crypto')` is gated by `NODE_FUNCTION_ALLOW_BUILTIN`). For HMAC (e.g. Slack signature verification in W_Abort) use the dedicated **Crypto node** (`n8n-nodes-base.crypto`, action `hmac`) and compare the result in a plain Code node — no builtin dependency.
- Per-segment WAV format: PCM 22050Hz mono 16-bit (44-byte standard header). `Build Full Audio Per Lang` relies on this being constant.
- **Sync workflow**: edit `code_nodes/*.js` → run `npm run sync` → commit both `.js` and `workflows/*.json`. If you edit a node inside the n8n UI, reverse-sync by copying the jsCode back into the `.js` file before committing. `npm run sync` only goes file → workflow.
